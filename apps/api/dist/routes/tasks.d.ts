import { Router, RequestHandler } from 'express';
export declare const processUploads: RequestHandler;
declare const router: Router;
export declare const handleChunks: RequestHandler;
export declare const normalizeArrays: RequestHandler;
export default router;
