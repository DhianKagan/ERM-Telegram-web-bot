🧠 AGENTS.md — рабочая инструкция для ассистентов (версия 3)

Версия: 3 — Рабочая, 2025-12-18
Назначение: однозначные, практичные и воспроизводимые правила для чат-ассистентов и Codex-агентов, заточенные под быструю и безопасную работу с кодом в этом репозитории.
Источник истины:

Машиночитаемо: .openai/assistant_instructions.json (обязательное чтение).

Человекочитаемо: этот файл (AGENTS.md) — подробный рабочий справочник. Всегда синхронизируй изменения в обоих файлах.

Коротко — что делать в первую очередь

Открой и прочитай .openai/assistant_instructions.json и AGENTS.md (эта версия). Они — единый набор правил.

Посчитай U/C (uncertainty/complexity). Если требования неясны — задать ровно 1–2 вопроса и остановиться.

Определи Mode: Lite | Standard | Research+ по правилам в JSON. В ответе обязательно укажи Mode: в первой строке.

Прежде чем что-то менять — прочитай все релевантные файлы (код, тесты, CI, конфиги) — правило read_before_write.

Чем отличается чат-агент и Codex

Чат-агент: анализ, планирование, предложения патчей/файлов, небольшие правки по запросу. Не запускает Codex-CI по умолчанию.

Codex (PR-агент): создаёт финальные PR/патчи, может выполнять CI-стадии (fast/full) в рамках политики session_limits и patch_policy. Codex требует pr.diff/плана перед автоматической работой и ограничен по сессиям.

Формат ответа — обязателен (строго)

Вся выдача (при решении задачи) должна идти в этом порядке:

Mode: <Lite|Standard|Research+>; Timezone: Europe/Kyiv

Assumptions: ≤3 (если есть)

Краткий диагноз: 1–3 строки — корень проблемы/цель

Plan: цели, шаги, риски (≤3), ожидаемый результат

Commands: точный набор команд (без destructive) для воспроизведения

Changes: unified patch (*** Begin Patch ... *** End Patch) и полные файлы (--- Begin File: path ... --- End File: path) — оба обязательны при изменениях кода

PR: branch name, commit message, body шаблон, чек-лист DoD, выдержки логов (если есть)

Chain-of-Verification: что проверено (build, tsc, tests, lint и т.д.)

Короткие объяснения по файлам: 2–4 предложения на файл

Рекомендации/альтернативы (если есть)

Инструкция для ревьювера/мерджа

Пример первой строки:
Mode: Standard; Timezone: Europe/Kyiv

Расчёт U/C (быстро)

U (uncertainty) — считай дискретно: 0 (ясно) или >0 (неясно). Если >0 — задай 1–2 уточняющих вопроса.

C (complexity) — 0..10:

+2 если тема нестабильна/актуальна (релизы, API, персоны и т.п.)

+2 если нужны код/тесты/PR

+1 за каждую подзадачу сверх первой (до +3)

+1 если требуются внешние инструменты (веб, gdrive, slack)

+3 если high-stakes (право/медицина/финансы)

+1 если требуются артефакты (диффы/патчи/скрины)

Mode:

Lite — C ≤ 2

Standard — 3 ≤ C ≤ 5 (или требуется код/PR)

Research+ — C ≥ 6 или high-stakes (требуется веб-проверка)

Обязательные команды (всегда запускать для проверки)

pnpm -w -s install

pnpm --filter shared build

pnpm -r --filter '!shared' build

pnpm --filter apps/api exec tsc -b

pnpm -w test

pnpm -w lint
Опционально (smoke): docker build -t ermbot:test -f Dockerfile .

Политика патчей (patch_policy)

Формат: cjs-patch в patch_cjs/ с именем NNN-description.cjs

Оркестратор: node patch_cjs/apply-all.cjs

После применения: pnpm install и pnpm -F web run prebuild

Роллбек: git restore <paths> (в JSON — hint)

Для Windows: проверять кроссплатформенность .cjs

Правила работы с кодом — практично

Минимализм. Меняй минимально необходимое.

Read before write. Перед предложением изменений открой все связанные файлы и тесты.

Тесты. При изменении бизнес-логики добавляй unit/integration тесты. Объясняй, почему тесты не добавлены, если таковое решение принято.

Config over code. Если проблему решает конфиг — изменяй конфиг.

TypeScript: при ошибке Property 'at' does not exist применять вариант A (arr[arr.length-1]) по умолчанию, если нет запроса на поднятие tsconfig.

Публичные типы: если много ошибок no exported member 'X' — добавлять экспорт в apps/api/src/db/model.ts, только если безопасно и минимально.

Логи: не добавляй шумных логов; включай requestId, userId и контекст если добавляешь лог.

CI: fast vs full

verify_fast (предпочтительно): короткая проверка — smoke build, lint, быстрые тесты (pnpm run ci:fast). Используй при changed_files < 50.

verify_full: полная CI для релизов/больших изменений (pnpm run ci:full).

DoD для PR: shared build OK, apps/api tsc OK, tests pass OR justified skip, lint OK.

Политика безопасности

Запрещено: destructive команды (rm -rf, git reset --hard...), публикация секретов, необоснованная обработка PII.

Отказ: при нарушении безопасности возвращай: Я не могу помочь с <X> из соображений безопасности. Предлагаю <альтернатива>.

Секреты: не коммить секреты в репо; тесты/CI должны использовать dummy-значения, как в README.

Список «не трогать» (обязателен)

apps/api/src/generated/**, packages/types/generated/** — правки только через генераторы.

Скрипты безопасности (scripts/check_mongo.mjs, scripts/audit_deps.sh) — не упрощать.

Конфигурации мониторинга (prometheus/, profiling/) — менять только с SRE.

docs/archive/** — не редактировать содержимое, лишь обновлять ссылки.

Стандарты оформления и соглашения

Branch name: fix/<short-description> или feat/<short-description> (lowercase, hyphen-separated).

Commit message: <type>(<scope>): short summary (например fix(api): allow null for location fields).

PR title/body: кратко что/почему; чек-лист DoD; выдержки логов. Пример шаблона есть в .openai/assistant_instructions.json.

Translation / messages: все текстовые сообщения бота в apps/api/src/messages.ts должны быть на русском.

Шаблоны — максимально практичные

Пример первой строки ответа агента:

Mode: Standard; Timezone: Europe/Kyiv
Assumptions:
1) ...
2) ...


Пример branch / commit / PR:

Branch: fix/null-location

Commit: fix(api): allow null for location fields

PR title: fix(api): allow null for location fields

PR body (короткий):

Что сделано: кратко

Почему: причина

DoD: чек-лист (build, tsc, tests, lint)

Как протестировать: шаги

Пример minimal system prompt (env):

Mode: Standard; Timezone: Europe/Kyiv
Смотрите .openai/assistant_instructions.json и AGENTS.md в корне репозитория — они содержат правила и workflow.


(Этот краткий промпт в окружении должен ссылаться на JSON; не дублируйте правила.)

Случаи неясности и согласование

Архитектурное изменение / shared API — требовать согласования. Предложи 2 варианта: минимальный (low-risk) и архитектурный (high-impact) с оценкой рисков и рекомендуемым вариантом.

Если входных данных мало — задать ровно 1–2 вопроса и ждать ответ (не продолжать).

Если задача high-stakes — пометить Research+ и выполнить веб-проверку/цитирование согласно JSON.

Проверки ревьювера / мерджера

JSON валиден и версия обновлена (если меняли).

Агент действительно читает .openai/assistant_instructions.json и AGENTS.md.

Unified patch + полные файлы присутствуют в PR (все изменённые файлы).

DoD: build/tsc/tests/lint пройдены или есть обоснование.

Нет утечек секретов или PII.

Если изменён API — присутствует пример запроса/ответа и миграционный план.

Что изменено по сравнению с краткой версией

Эта версия — рабочая, расширенная: добавлены детальные правила, шаблоны, чек-листы, сценарии CI, список «не трогать», практические шаблоны для branch/commit/PR, и чёткая инструкция на случай неясностей.

Короткая версия хороша как «one-liner», но для повседневной работы агентов (и людей) требуется именно такая подробная, воспроизводимая инструкция.

Версия и поддержка

Обновляй JSON если меняются правила выполнения/CI/patch_policy.

Обновляй AGENTS.md для рабочих примеров и пояснений. Синхронизируй оба файла при изменениях.
