# Назначение файла: пример конфигурации конвейера логов Railway.
# Скопируйте этот файл в Railway/config/pipeline.env и заполните параметры.
# Значения используются скриптом scripts/railway_log_pipeline.sh.

# Имя сервиса Railway, для которого нужно собирать логи.
RAILWAY_SERVICE=erm-api
# Имя окружения в Railway (например, production, staging).
RAILWAY_ENVIRONMENT=production
# Какой деплой анализировать: latest или конкретный идентификатор.
RAILWAY_DEPLOY=latest
# Количество строк, которые нужно запросить из логов.
RAILWAY_TAIL=400
