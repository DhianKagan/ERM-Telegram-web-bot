# включаем PMTiles режим (убираем raster fallback)
VITE_USE_PMTILES=1            # или просто удалить VITE_USE_PMTILES=0

# включаем SSE (если сервер поддерживает)
VITE_DISABLE_SSE=0

# указываем адрес локальной pmtiles (важно для клиента/билда)
VITE_MAP_ADDRESSES_PMTILES_URL=pmtiles://tiles/addresses.pmtiles

# репомические спрайты/шрифты Protomaps — разрешаем в CSP
CSP_CONNECT_SRC_ALLOWLIST=https://protomaps.github.io
CSP_FONT_SRC_ALLOWLIST=https://protomaps.github.io
CSP_IMG_SRC_ALLOWLIST=https://protomaps.github.io
